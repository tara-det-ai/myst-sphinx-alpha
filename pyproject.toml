[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Determined AI Sphinx Theme Test"
authors = [{name = "DeterminedAI", email = "determined@determined"}]
dynamic = ["version", "description"]
